let map = fun(arr, f) {
    let iter = fun(arr, acc) {
        if (length(arr) == 0) {
            acc
        } else {
            iter(rest(arr), push(acc, f(first(arr))));
        }
    }; 
    iter(arr, []);
};