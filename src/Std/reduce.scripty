let reduce = fun(arr, initial, f) {
    let iter = fun(arr, result) {
        if (length(arr) == 0) {
            result
        } else {
            iter(rest(arr), f(result, first(arr)));
        }
    };

    iter(arr, initial);
};